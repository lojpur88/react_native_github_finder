import { Dimensions } from 'react-native';

export const { width: WIDTH, height: HEIGHT } = Dimensions.get('screen');
export const COLORS = {
  dark: '#0D1117',
  darkGrey: '#30363D',
  lightGrey: '#E6E6E6',
};
